# AM Check In 1

Canonical source repository: `Python-Level-3`

This project consolidates the legacy `AM-Check-In-1` and `AM-Check-In-1-Starter` folders into the common course wrapper layout.

## Structure

- `starter/` contains the starter/template snapshot.
- `solution/` contains the solution/reference snapshot.
